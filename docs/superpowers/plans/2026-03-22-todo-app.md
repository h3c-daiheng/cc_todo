# 待办应用实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个前后端分离的局域网待办应用，支持个人任务管理、团队协作、附件上传和每日邮件提醒。

**Architecture:** 后端使用 FastAPI + SQLAlchemy + SQLite（WAL 模式），通过 JWT 认证；前端使用 Vue 3 + Element Plus，通过 axios 调用 REST API；APScheduler 每日定时发送邮件提醒。

**Tech Stack:** Python 3.11+, FastAPI, SQLAlchemy 2.x, APScheduler, python-jose, bcrypt, python-magic, Vue 3, Vite, Element Plus, Pinia, Vue Draggable, axios

**Spec:** `docs/superpowers/specs/2026-03-22-todo-app-design.md`

---

## 文件结构总览

```
todo-app/
├── backend/
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── models.py
│   ├── schemas.py
│   ├── dependencies.py
│   ├── routers/
│   │   ├── auth.py
│   │   ├── users.py
│   │   ├── tasks.py
│   │   ├── teams.py
│   │   ├── comments.py
│   │   ├── attachments.py
│   │   └── settings.py
│   ├── services/
│   │   ├── auth.py
│   │   └── email.py
│   ├── scheduler.py
│   ├── scripts/
│   │   └── init_admin.py
│   ├── requirements.txt
│   └── tests/
│       ├── conftest.py
│       ├── test_auth.py
│       ├── test_tasks.py
│       ├── test_teams.py
│       └── test_attachments.py
└── frontend/
    ├── package.json
    ├── vite.config.js
    ├── index.html
    └── src/
        ├── main.js
        ├── App.vue
        ├── router/index.js
        ├── api/index.js
        ├── stores/user.js
        ├── stores/task.js
        └── views/
            ├── Login.vue
            ├── MyTasks.vue
            ├── TeamTasks.vue
            ├── TaskDetail.vue
            ├── TeamManage.vue
            └── admin/
                ├── UserManage.vue
                └── SystemSettings.vue
```

---

## Task 1: 后端项目初始化

**Files:**
- Create: `backend/requirements.txt`
- Create: `backend/config.py`
- Create: `backend/database.py`

- [ ] **Step 1: 创建后端目录结构**

```bash
mkdir -p backend/routers backend/services backend/scripts backend/tests
touch backend/__init__.py backend/routers/__init__.py backend/services/__init__.py
```

- [ ] **Step 2: 创建 requirements.txt**

```
fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.36
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
python-magic==0.4.27
apscheduler==3.10.4
cryptography==43.0.0
pytest==8.3.0
pytest-asyncio==0.24.0
httpx==0.27.0
```

- [ ] **Step 3: 安装系统依赖和 Python 依赖**

```bash
# Ubuntu/Debian 需要先安装 libmagic（python-magic 的底层依赖）
apt-get install -y libmagic1

cd backend && pip install -r requirements.txt
```

- [ ] **Step 4: 创建 config.py**

```python
# backend/config.py
import os
from pathlib import Path

SECRET_KEY = os.environ.get("TODO_SECRET_KEY", "dev-secret-key-change-in-prod")
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 480   # 8 小时
REFRESH_TOKEN_EXPIRE_DAYS = 7

UPLOAD_DIR = Path(os.environ.get("TODO_UPLOAD_DIR", "./uploads"))
UPLOAD_DIR.mkdir(parents=True, exist_ok=True)

MAX_UPLOAD_SIZE = 20 * 1024 * 1024  # 20MB

ALLOWED_MIME_TYPES = {
    "image/jpeg", "image/png", "image/gif",
    "application/pdf",
    "application/msword",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "application/vnd.ms-excel",
    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    "application/vnd.ms-powerpoint",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation",
}

DATABASE_URL = "sqlite:///./todo.db"
```

- [ ] **Step 5: 创建 database.py（WAL 模式）**

```python
# backend/database.py
from sqlalchemy import create_engine, event, text
from sqlalchemy.orm import sessionmaker, DeclarativeBase
from config import DATABASE_URL

engine = create_engine(
    DATABASE_URL,
    connect_args={"check_same_thread": False},
)

@event.listens_for(engine, "connect")
def set_wal_mode(dbapi_conn, _):
    dbapi_conn.execute("PRAGMA journal_mode=WAL")
    dbapi_conn.execute("PRAGMA foreign_keys=ON")

SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

class Base(DeclarativeBase):
    pass

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

- [ ] **Step 6: 写测试验证 WAL 模式开启**

```python
# backend/tests/test_database.py
from sqlalchemy import text
from database import engine

def test_wal_mode_enabled():
    with engine.connect() as conn:
        result = conn.execute(text("PRAGMA journal_mode")).scalar()
    assert result == "wal"

def test_foreign_keys_enabled():
    with engine.connect() as conn:
        result = conn.execute(text("PRAGMA foreign_keys")).scalar()
    assert result == 1
```

- [ ] **Step 7: 运行测试（预期通过 — WAL 测试不依赖模型，只检查 PRAGMA）**

```bash
cd backend && python -c "from database import create_tables; create_tables()" && pytest tests/test_database.py -v
```

- [ ] **Step 8: Commit**

```bash
git add backend/ && git commit -m "feat: backend project init with config and WAL database"
```

---

## Task 2: 数据库模型

**Files:**
- Create: `backend/models.py`

- [ ] **Step 1: 编写 models.py**

```python
# backend/models.py
from datetime import datetime, date
from sqlalchemy import (
    Integer, String, Boolean, DateTime, Date, Text,
    ForeignKey, UniqueConstraint
)
from sqlalchemy.orm import Mapped, mapped_column, relationship
from database import Base

class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    username: Mapped[str] = mapped_column(String(64), unique=True, nullable=False)
    password_hash: Mapped[str] = mapped_column(String(256), nullable=False)
    email: Mapped[str] = mapped_column(String(256), unique=True, nullable=False)
    is_admin: Mapped[bool] = mapped_column(Boolean, default=False)
    email_notify: Mapped[bool] = mapped_column(Boolean, default=True)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

    tasks_created = relationship("Task", foreign_keys="Task.created_by", back_populates="creator")
    tasks_assigned = relationship("Task", foreign_keys="Task.assigned_to", back_populates="assignee")
    teams_led = relationship("Team", back_populates="leader")
    memberships = relationship("TeamMember", back_populates="user")
    comments = relationship("Comment", back_populates="author")
    attachments = relationship("Attachment", back_populates="uploader")

class Team(Base):
    __tablename__ = "teams"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    name: Mapped[str] = mapped_column(String(128), nullable=False)
    created_by: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

    leader = relationship("User", back_populates="teams_led")
    members = relationship("TeamMember", back_populates="team")
    tasks = relationship("Task", back_populates="team")

class TeamMember(Base):
    __tablename__ = "team_members"
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), primary_key=True)
    team_id: Mapped[int] = mapped_column(Integer, ForeignKey("teams.id"), primary_key=True)
    joined_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

    user = relationship("User", back_populates="memberships")
    team = relationship("Team", back_populates="members")

class Task(Base):
    __tablename__ = "tasks"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    title: Mapped[str] = mapped_column(String(256), nullable=False)
    description: Mapped[str | None] = mapped_column(Text)
    status: Mapped[str] = mapped_column(String(32), nullable=False, default="pending")
    priority: Mapped[str] = mapped_column(String(16), nullable=False, default="medium")
    due_date: Mapped[date | None] = mapped_column(Date)
    created_by: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    assigned_to: Mapped[int | None] = mapped_column(Integer, ForeignKey("users.id"))
    team_id: Mapped[int | None] = mapped_column(Integer, ForeignKey("teams.id"))
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    creator = relationship("User", foreign_keys=[created_by], back_populates="tasks_created")
    assignee = relationship("User", foreign_keys=[assigned_to], back_populates="tasks_assigned")
    team = relationship("Team", back_populates="tasks")
    labels = relationship("TaskLabel", back_populates="task", cascade="all, delete-orphan")
    comments = relationship("Comment", back_populates="task", cascade="all, delete-orphan")
    attachments = relationship("Attachment", back_populates="task", cascade="all, delete-orphan")

class TaskLabel(Base):
    __tablename__ = "task_labels"
    task_id: Mapped[int] = mapped_column(Integer, ForeignKey("tasks.id"), primary_key=True)
    label: Mapped[str] = mapped_column(String(64), primary_key=True)

    task = relationship("Task", back_populates="labels")

class Comment(Base):
    __tablename__ = "comments"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    task_id: Mapped[int] = mapped_column(Integer, ForeignKey("tasks.id"), nullable=False)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

    task = relationship("Task", back_populates="comments")
    author = relationship("User", back_populates="comments")

class Attachment(Base):
    __tablename__ = "attachments"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    task_id: Mapped[int] = mapped_column(Integer, ForeignKey("tasks.id"), nullable=False)
    uploaded_by: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    filename: Mapped[str] = mapped_column(String(256), nullable=False)
    stored_name: Mapped[str] = mapped_column(String(256), nullable=False, unique=True)
    file_size: Mapped[int] = mapped_column(Integer, nullable=False)
    mime_type: Mapped[str] = mapped_column(String(128), nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

    task = relationship("Task", back_populates="attachments")
    uploader = relationship("User", back_populates="attachments")

class SystemSetting(Base):
    __tablename__ = "system_settings"
    key: Mapped[str] = mapped_column(String(64), primary_key=True)
    value: Mapped[str | None] = mapped_column(Text)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

- [ ] **Step 2: 更新 database.py — 添加建表调用，更新 test_database.py**

在 `database.py` 末尾添加：
```python
def create_tables():
    import models  # noqa: F401 — 确保所有模型已加载
    Base.metadata.create_all(bind=engine)
```

- [ ] **Step 3: 运行数据库测试（现在应该通过）**

```bash
cd backend && python -c "from database import create_tables; create_tables()" && pytest tests/test_database.py -v
```

Expected: 2 passed

- [ ] **Step 4: Commit**

```bash
git add backend/ && git commit -m "feat: add SQLAlchemy models for all tables"
```

---

## Task 3: 认证服务

**Files:**
- Create: `backend/services/auth.py`
- Create: `backend/schemas.py`（认证部分）

- [ ] **Step 1: 编写认证服务测试**

```python
# backend/tests/test_auth_service.py
from services.auth import hash_password, verify_password, create_access_token, decode_token

def test_password_hash_and_verify():
    pwd = "MySecret123"
    hashed = hash_password(pwd)
    assert hashed != pwd
    assert verify_password(pwd, hashed)
    assert not verify_password("wrong", hashed)

def test_access_token_roundtrip():
    token = create_access_token({"sub": "42"})
    payload = decode_token(token)
    assert payload["sub"] == "42"

def test_expired_token_raises():
    from services.auth import create_token_with_expiry
    from datetime import timedelta
    import time
    token = create_token_with_expiry({"sub": "1"}, timedelta(seconds=-1))
    payload = decode_token(token)
    assert payload is None
```

- [ ] **Step 2: 运行测试（预期失败）**

```bash
cd backend && pytest tests/test_auth_service.py -v
```

- [ ] **Step 3: 实现 services/auth.py**

```python
# backend/services/auth.py
from datetime import datetime, timedelta, timezone
from typing import Any
from passlib.context import CryptContext
from jose import JWTError, jwt
from config import SECRET_KEY, ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES, REFRESH_TOKEN_EXPIRE_DAYS

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)

def create_token_with_expiry(data: dict, expires_delta: timedelta) -> str:
    payload = data.copy()
    payload["exp"] = datetime.now(timezone.utc) + expires_delta
    return jwt.encode(payload, SECRET_KEY, algorithm=ALGORITHM)

def create_access_token(data: dict) -> str:
    return create_token_with_expiry(data, timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES))

def create_refresh_token(data: dict) -> str:
    return create_token_with_expiry(data, timedelta(days=REFRESH_TOKEN_EXPIRE_DAYS))

def decode_token(token: str) -> dict | None:
    try:
        return jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    except JWTError:
        return None
```

- [ ] **Step 4: 运行测试（预期通过）**

```bash
cd backend && pytest tests/test_auth_service.py -v
```

Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add backend/ && git commit -m "feat: add auth service with bcrypt and JWT"
```

---

## Task 4: 依赖注入与权限中间件

**Files:**
- Create: `backend/dependencies.py`

- [ ] **Step 1: 编写 dependencies.py**

```python
# backend/dependencies.py
from collections import defaultdict
from datetime import datetime, timezone
from fastapi import Depends, HTTPException, Cookie, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.orm import Session
from database import get_db
from models import User
from services.auth import decode_token

bearer = HTTPBearer(auto_error=False)

# 内存黑名单（refresh token 注销后加入）
_blacklist: set[str] = set()

# 暴力破解防护：{ip: [(timestamp, success), ...]}
_login_attempts: dict[str, list] = defaultdict(list)

def blacklist_token(token: str):
    _blacklist.add(token)

def is_blacklisted(token: str) -> bool:
    return token in _blacklist

def record_login_attempt(ip: str, success: bool):
    now = datetime.now(timezone.utc).timestamp()
    attempts = _login_attempts[ip]
    # 只保留最近 5 分钟的记录
    _login_attempts[ip] = [a for a in attempts if now - a[0] < 300] + [(now, success)]

def is_ip_locked(ip: str) -> bool:
    now = datetime.now(timezone.utc).timestamp()
    recent = [a for a in _login_attempts[ip] if now - a[0] < 300 and not a[1]]
    if len(recent) >= 10:
        # 最后一次失败是否在 15 分钟内
        last_fail = max(a[0] for a in recent)
        return (now - last_fail) < 900
    return False

def get_current_user(
    credentials: HTTPAuthorizationCredentials | None = Depends(bearer),
    db: Session = Depends(get_db),
) -> User:
    if not credentials:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="未登录")
    payload = decode_token(credentials.credentials)
    if not payload:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Token 无效或已过期")
    user_id = payload.get("sub")
    user = db.get(User, int(user_id))
    if not user or not user.is_active:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="账号不存在或已停用")
    return user

def require_admin(current_user: User = Depends(get_current_user)) -> User:
    if not current_user.is_admin:
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="需要管理员权限")
    return current_user
```

- [ ] **Step 2: 编写 dependencies 测试**

```python
# backend/tests/test_dependencies.py
from dependencies import (
    record_login_attempt, is_ip_locked,
    blacklist_token, is_blacklisted,
    _login_attempts
)

def test_ip_not_locked_initially():
    assert not is_ip_locked("1.2.3.4")

def test_ip_locked_after_10_failures():
    ip = "10.0.0.1"
    _login_attempts.pop(ip, None)
    for _ in range(10):
        record_login_attempt(ip, False)
    assert is_ip_locked(ip)

def test_ip_not_locked_after_successes():
    ip = "10.0.0.2"
    _login_attempts.pop(ip, None)
    for _ in range(5):
        record_login_attempt(ip, True)
    assert not is_ip_locked(ip)

def test_blacklist_token():
    token = "some.jwt.token"
    assert not is_blacklisted(token)
    blacklist_token(token)
    assert is_blacklisted(token)
```

- [ ] **Step 3: 运行 dependencies 测试**

```bash
cd backend && pytest tests/test_dependencies.py -v
```

Expected: 4 passed

- [ ] **Step 4: Commit**

```bash
git add backend/dependencies.py backend/tests/test_dependencies.py && git commit -m "feat: add dependency injection with auth, rate limiting, blacklist"
```

---

## Task 5: 认证路由（登录/注销/刷新）

**Files:**
- Create: `backend/routers/auth.py`
- Create: `backend/tests/conftest.py`
- Create: `backend/tests/test_auth.py`

- [ ] **Step 1: 创建测试 conftest.py**

```python
# backend/tests/conftest.py
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from database import Base, get_db
from main import app
from models import User
from services.auth import hash_password

TEST_DB = "sqlite:///./test.db"
engine = create_engine(TEST_DB, connect_args={"check_same_thread": False})
TestingSession = sessionmaker(bind=engine)

@pytest.fixture(autouse=True)
def setup_db():
    Base.metadata.create_all(bind=engine)
    yield
    Base.metadata.drop_all(bind=engine)

@pytest.fixture
def db():
    session = TestingSession()
    try:
        yield session
    finally:
        session.close()

@pytest.fixture
def client(db):
    def override_db():
        yield db
    app.dependency_overrides[get_db] = override_db
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()

@pytest.fixture
def admin_user(db):
    user = User(
        username="admin", email="admin@test.com",
        password_hash=hash_password("admin123"), is_admin=True
    )
    db.add(user); db.commit(); db.refresh(user)
    return user

@pytest.fixture
def normal_user(db):
    user = User(
        username="alice", email="alice@test.com",
        password_hash=hash_password("alice123")
    )
    db.add(user); db.commit(); db.refresh(user)
    return user

@pytest.fixture
def auth_headers(client, normal_user):
    resp = client.post("/api/v1/auth/login", json={"username": "alice", "password": "alice123"})
    token = resp.json()["data"]["access_token"]
    return {"Authorization": f"Bearer {token}"}
```

- [ ] **Step 2: 编写 auth 路由测试**

```python
# backend/tests/test_auth.py
def test_login_success(client, normal_user):
    resp = client.post("/api/v1/auth/login", json={"username": "alice", "password": "alice123"})
    assert resp.status_code == 200
    data = resp.json()["data"]
    assert "access_token" in data

def test_login_wrong_password(client, normal_user):
    resp = client.post("/api/v1/auth/login", json={"username": "alice", "password": "wrong"})
    assert resp.status_code == 401

def test_login_inactive_user(client, db, normal_user):
    normal_user.is_active = False
    db.commit()
    resp = client.post("/api/v1/auth/login", json={"username": "alice", "password": "alice123"})
    assert resp.status_code == 401

def test_logout(client, auth_headers):
    resp = client.post("/api/v1/auth/logout", headers=auth_headers)
    assert resp.status_code == 200
```

- [ ] **Step 3: 实现 routers/auth.py**

```python
# backend/routers/auth.py
from fastapi import APIRouter, Depends, HTTPException, Request, Response, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from pydantic import BaseModel
from sqlalchemy.orm import Session
from database import get_db
from models import User
from services.auth import verify_password, create_access_token, create_refresh_token, decode_token
from dependencies import (
    blacklist_token, is_blacklisted, record_login_attempt,
    is_ip_locked, get_current_user
)

router = APIRouter(prefix="/auth", tags=["auth"])

class LoginRequest(BaseModel):
    username: str
    password: str

def ok(data):
    return {"code": 0, "message": "ok", "data": data}

@router.post("/login")
def login(req: LoginRequest, request: Request, response: Response, db: Session = Depends(get_db)):
    ip = request.client.host
    if is_ip_locked(ip):
        raise HTTPException(status_code=429, detail="登录尝试过于频繁，请 15 分钟后再试")
    user = db.query(User).filter(User.username == req.username).first()
    if not user or not user.is_active or not verify_password(req.password, user.password_hash):
        record_login_attempt(ip, False)
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="用户名或密码错误")
    record_login_attempt(ip, True)
    access_token = create_access_token({"sub": str(user.id)})
    refresh_token = create_refresh_token({"sub": str(user.id), "type": "refresh"})
    response.set_cookie("refresh_token", refresh_token, httponly=True, max_age=7 * 86400)
    return ok({"access_token": access_token, "token_type": "bearer"})

@router.post("/logout")
def logout(response: Response, current_user: User = Depends(get_current_user),
           refresh_token: str | None = None):
    if refresh_token:
        blacklist_token(refresh_token)
    response.delete_cookie("refresh_token")
    return ok(None)

@router.post("/refresh")
def refresh(request: Request, response: Response, db: Session = Depends(get_db)):
    token = request.cookies.get("refresh_token")
    if not token or is_blacklisted(token):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="refresh token 无效")
    payload = decode_token(token)
    if not payload or payload.get("type") != "refresh":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="refresh token 无效")
    user = db.get(User, int(payload["sub"]))
    if not user or not user.is_active:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="账号不存在或已停用")
    new_access = create_access_token({"sub": str(user.id)})
    return ok({"access_token": new_access})
```

- [ ] **Step 4: 创建 main.py**

```python
# backend/main.py
from fastapi import FastAPI
from database import create_tables
from routers import auth, tasks, teams, comments, attachments, users, settings

app = FastAPI(title="Todo App API")

@app.on_event("startup")
def startup():
    create_tables()

app.include_router(auth.router, prefix="/api/v1")
app.include_router(tasks.router, prefix="/api/v1")
app.include_router(teams.router, prefix="/api/v1")
app.include_router(comments.router, prefix="/api/v1")
app.include_router(attachments.router, prefix="/api/v1")
app.include_router(users.router, prefix="/api/v1")
app.include_router(settings.router, prefix="/api/v1")
```

- [ ] **Step 5: 创建空路由桩（让 main.py 能导入）**

```bash
for f in tasks teams comments attachments users settings; do
  echo "from fastapi import APIRouter; router = APIRouter()" > backend/routers/${f}.py
done
```

- [ ] **Step 6: 运行 auth 测试**

```bash
cd backend && pytest tests/test_auth.py -v
```

Expected: 4 passed

- [ ] **Step 7: Commit**

```bash
git add backend/ && git commit -m "feat: add auth routes login/logout/refresh with brute force protection"
```

---

## Task 6: 任务 CRUD 路由

**Files:**
- Create: `backend/routers/tasks.py`
- Create: `backend/tests/test_tasks.py`

- [ ] **Step 1: 编写任务测试**

```python
# backend/tests/test_tasks.py
def test_create_personal_task(client, auth_headers):
    resp = client.post("/api/v1/tasks", json={
        "title": "买菜", "priority": "high"
    }, headers=auth_headers)
    assert resp.status_code == 200
    assert resp.json()["data"]["title"] == "买菜"

def test_list_my_tasks(client, auth_headers):
    client.post("/api/v1/tasks", json={"title": "任务1"}, headers=auth_headers)
    resp = client.get("/api/v1/tasks", headers=auth_headers)
    assert resp.status_code == 200
    assert len(resp.json()["data"]["items"]) == 1

def test_update_task_status(client, auth_headers):
    r = client.post("/api/v1/tasks", json={"title": "任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    resp = client.patch(f"/api/v1/tasks/{tid}/status",
                        json={"status": "in_progress"}, headers=auth_headers)
    assert resp.status_code == 200
    assert resp.json()["data"]["status"] == "in_progress"

def test_delete_task(client, auth_headers):
    r = client.post("/api/v1/tasks", json={"title": "临时任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    resp = client.delete(f"/api/v1/tasks/{tid}", headers=auth_headers)
    assert resp.status_code == 200

def test_other_user_cannot_delete_task(client, db, auth_headers):
    from models import User; from services.auth import hash_password
    bob = User(username="bob", email="bob@t.com", password_hash=hash_password("bob123"))
    db.add(bob); db.commit()
    r = client.post("/api/v1/tasks", json={"title": "Alice任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    bob_resp = client.post("/api/v1/auth/login", json={"username": "bob", "password": "bob123"})
    bob_token = bob_resp.json()["data"]["access_token"]
    resp = client.delete(f"/api/v1/tasks/{tid}", headers={"Authorization": f"Bearer {bob_token}"})
    assert resp.status_code == 403
```

- [ ] **Step 2: 实现 routers/tasks.py**

```python
# backend/routers/tasks.py
from datetime import date
from fastapi import APIRouter, Depends, HTTPException, Query, status
from pydantic import BaseModel
from sqlalchemy.orm import Session
from database import get_db
from models import Task, TaskLabel, TeamMember
from dependencies import get_current_user
from models import User

router = APIRouter(prefix="/tasks", tags=["tasks"])

def ok(data): return {"code": 0, "message": "ok", "data": data}

def task_to_dict(task: Task) -> dict:
    return {
        "id": task.id, "title": task.title, "description": task.description,
        "status": task.status, "priority": task.priority,
        "due_date": task.due_date.isoformat() if task.due_date else None,
        "created_by": task.created_by, "assigned_to": task.assigned_to,
        "team_id": task.team_id,
        "labels": [l.label for l in task.labels],
        "created_at": task.created_at.isoformat(),
        "updated_at": task.updated_at.isoformat(),
    }

def can_view_task(task: Task, user: User, db: Session) -> bool:
    if task.created_by == user.id or task.assigned_to == user.id or user.is_admin:
        return True
    if task.team_id:
        return db.query(TeamMember).filter_by(user_id=user.id, team_id=task.team_id).first() is not None
    return False

def can_edit_task(task: Task, user: User, db) -> bool:
    if user.is_admin: return True
    if task.created_by == user.id: return True
    if task.team_id:
        from models import Team
        team = db.get(Team, task.team_id)
        if team and team.created_by == user.id: return True
    return False

class TaskCreate(BaseModel):
    title: str
    description: str | None = None
    priority: str = "medium"
    due_date: date | None = None
    assigned_to: int | None = None
    team_id: int | None = None
    labels: list[str] = []

class TaskUpdate(BaseModel):
    title: str | None = None
    description: str | None = None
    priority: str | None = None
    due_date: date | None = None
    labels: list[str] | None = None

class StatusUpdate(BaseModel):
    status: str

class AssigneeUpdate(BaseModel):
    assigned_to: int | None

@router.post("")
def create_task(body: TaskCreate, db: Session = Depends(get_db),
                current_user: User = Depends(get_current_user)):
    task = Task(
        title=body.title, description=body.description,
        priority=body.priority, due_date=body.due_date,
        created_by=current_user.id, assigned_to=body.assigned_to,
        team_id=body.team_id,
    )
    db.add(task); db.flush()
    for label in body.labels:
        db.add(TaskLabel(task_id=task.id, label=label))
    db.commit(); db.refresh(task)
    return ok(task_to_dict(task))

@router.get("")
def list_tasks(
    page: int = 1, size: int = 20,
    status: str | None = None, priority: str | None = None,
    assigned_to: int | None = None, label: str | None = None,
    db: Session = Depends(get_db), current_user: User = Depends(get_current_user)
):
    q = db.query(Task).filter(
        (Task.created_by == current_user.id) | (Task.assigned_to == current_user.id)
    )
    if status: q = q.filter(Task.status == status)
    if priority: q = q.filter(Task.priority == priority)
    if assigned_to: q = q.filter(Task.assigned_to == assigned_to)
    if label: q = q.join(TaskLabel).filter(TaskLabel.label == label)
    total = q.count()
    items = q.offset((page - 1) * size).limit(size).all()
    return ok({"total": total, "page": page, "size": size, "items": [task_to_dict(t) for t in items]})

@router.get("/{task_id}")
def get_task(task_id: int, db: Session = Depends(get_db),
             current_user: User = Depends(get_current_user)):
    task = db.get(Task, task_id)
    if not task or not can_view_task(task, current_user, db):
        raise HTTPException(status_code=404, detail="任务不存在")
    return ok(task_to_dict(task))

@router.put("/{task_id}")
def update_task(task_id: int, body: TaskUpdate, db: Session = Depends(get_db),
                current_user: User = Depends(get_current_user)):
    task = db.get(Task, task_id)
    if not task or not can_view_task(task, current_user, db):
        raise HTTPException(status_code=404, detail="任务不存在")
    if not can_edit_task(task, current_user, db):
        raise HTTPException(status_code=403, detail="无权限编辑此任务")
    if body.title is not None: task.title = body.title
    if body.description is not None: task.description = body.description
    if body.priority is not None: task.priority = body.priority
    if body.due_date is not None: task.due_date = body.due_date
    if body.labels is not None:
        for l in task.labels: db.delete(l)
        db.flush()
        for label in body.labels:
            db.add(TaskLabel(task_id=task.id, label=label))
    db.commit(); db.refresh(task)
    return ok(task_to_dict(task))

@router.patch("/{task_id}/status")
def update_status(task_id: int, body: StatusUpdate, db: Session = Depends(get_db),
                  current_user: User = Depends(get_current_user)):
    valid = {"pending", "in_progress", "done"}
    if body.status not in valid:
        raise HTTPException(status_code=400, detail=f"状态必须是 {valid} 之一")
    task = db.get(Task, task_id)
    if not task or not can_view_task(task, current_user, db):
        raise HTTPException(status_code=404, detail="任务不存在")
    if not (task.created_by == current_user.id or task.assigned_to == current_user.id
            or can_edit_task(task, current_user, db)):
        raise HTTPException(status_code=403, detail="无权限修改状态")
    task.status = body.status
    db.commit(); db.refresh(task)
    return ok(task_to_dict(task))

@router.patch("/{task_id}/assignee")
def update_assignee(task_id: int, body: AssigneeUpdate, db: Session = Depends(get_db),
                    current_user: User = Depends(get_current_user)):
    task = db.get(Task, task_id)
    if not task or not can_view_task(task, current_user, db):
        raise HTTPException(status_code=404, detail="任务不存在")
    if not can_edit_task(task, current_user, db):
        raise HTTPException(status_code=403, detail="无权限分配负责人")
    task.assigned_to = body.assigned_to
    db.commit(); db.refresh(task)
    return ok(task_to_dict(task))

@router.delete("/{task_id}")
def delete_task(task_id: int, db: Session = Depends(get_db),
                current_user: User = Depends(get_current_user)):
    task = db.get(Task, task_id)
    if not task or not can_view_task(task, current_user, db):
        raise HTTPException(status_code=404, detail="任务不存在")
    if not can_edit_task(task, current_user, db):
        raise HTTPException(status_code=403, detail="无权限删除此任务")
    db.delete(task); db.commit()
    return ok(None)
```

- [ ] **Step 3: 运行任务测试**

```bash
cd backend && pytest tests/test_tasks.py -v
```

Expected: 5 passed

- [ ] **Step 4: Commit**

```bash
git add backend/ && git commit -m "feat: add task CRUD routes with permission checks"
```

---

## Task 7: 团队路由

**Files:**
- Create: `backend/routers/teams.py`
- Create: `backend/tests/test_teams.py`

- [ ] **Step 1: 编写团队测试**

```python
# backend/tests/test_teams.py
def test_create_team(client, auth_headers):
    resp = client.post("/api/v1/teams", json={"name": "开发组"}, headers=auth_headers)
    assert resp.status_code == 200
    assert resp.json()["data"]["name"] == "开发组"

def test_get_team_detail(client, auth_headers):
    r = client.post("/api/v1/teams", json={"name": "开发组"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    resp = client.get(f"/api/v1/teams/{tid}", headers=auth_headers)
    assert resp.status_code == 200
    assert "members" in resp.json()["data"]

def test_add_and_remove_member(client, db, auth_headers):
    from models import User; from services.auth import hash_password
    bob = User(username="bob", email="bob@t.com", password_hash=hash_password("bob123"))
    db.add(bob); db.commit()
    r = client.post("/api/v1/teams", json={"name": "Team"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    resp = client.post(f"/api/v1/teams/{tid}/members",
                       json={"user_id": bob.id}, headers=auth_headers)
    assert resp.status_code == 200
    resp2 = client.delete(f"/api/v1/teams/{tid}/members/{bob.id}", headers=auth_headers)
    assert resp2.status_code == 200
```

- [ ] **Step 2: 实现 routers/teams.py**

```python
# backend/routers/teams.py
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from sqlalchemy.orm import Session
from database import get_db
from models import Team, TeamMember, Task, User
from dependencies import get_current_user

router = APIRouter(prefix="/teams", tags=["teams"])

def ok(data): return {"code": 0, "message": "ok", "data": data}

def is_team_leader(team: Team, user: User) -> bool:
    return team.created_by == user.id or user.is_admin

def is_team_member(team_id: int, user_id: int, db: Session) -> bool:
    return db.query(TeamMember).filter_by(team_id=team_id, user_id=user_id).first() is not None

class TeamCreate(BaseModel):
    name: str

class AddMember(BaseModel):
    user_id: int

@router.get("")
def list_my_teams(db: Session = Depends(get_db), current_user: User = Depends(get_current_user)):
    memberships = db.query(TeamMember).filter_by(user_id=current_user.id).all()
    led = db.query(Team).filter_by(created_by=current_user.id).all()
    team_ids = {m.team_id for m in memberships} | {t.id for t in led}
    teams = db.query(Team).filter(Team.id.in_(team_ids)).all()
    return ok([{"id": t.id, "name": t.name, "created_by": t.created_by} for t in teams])

@router.post("")
def create_team(body: TeamCreate, db: Session = Depends(get_db),
                current_user: User = Depends(get_current_user)):
    team = Team(name=body.name, created_by=current_user.id)
    db.add(team); db.flush()
    db.add(TeamMember(team_id=team.id, user_id=current_user.id))
    db.commit(); db.refresh(team)
    return ok({"id": team.id, "name": team.name, "created_by": team.created_by})

@router.get("/{team_id}")
def get_team(team_id: int, db: Session = Depends(get_db),
             current_user: User = Depends(get_current_user)):
    team = db.get(Team, team_id)
    if not team:
        raise HTTPException(status_code=404, detail="团队不存在")
    if not is_team_member(team_id, current_user.id, db) and not current_user.is_admin:
        raise HTTPException(status_code=403, detail="无权限")
    members = [{
        "user_id": m.user_id,
        "username": m.user.username,
        "email": m.user.email,
        "joined_at": m.joined_at.isoformat()
    } for m in team.members]
    return ok({"id": team.id, "name": team.name, "created_by": team.created_by, "members": members})

@router.get("/{team_id}/tasks")
def list_team_tasks(team_id: int, page: int = 1, size: int = 20,
                    db: Session = Depends(get_db), current_user: User = Depends(get_current_user)):
    team = db.get(Team, team_id)
    if not team:
        raise HTTPException(status_code=404, detail="团队不存在")
    if not is_team_member(team_id, current_user.id, db) and not current_user.is_admin:
        raise HTTPException(status_code=403, detail="无权限")
    q = db.query(Task).filter_by(team_id=team_id)
    total = q.count()
    items = q.offset((page-1)*size).limit(size).all()
    from routers.tasks import task_to_dict
    return ok({"total": total, "page": page, "size": size, "items": [task_to_dict(t) for t in items]})

@router.post("/{team_id}/members")
def add_member(team_id: int, body: AddMember, db: Session = Depends(get_db),
               current_user: User = Depends(get_current_user)):
    team = db.get(Team, team_id)
    if not team: raise HTTPException(status_code=404, detail="团队不存在")
    if not is_team_leader(team, current_user):
        raise HTTPException(status_code=403, detail="仅团队负责人可操作")
    if is_team_member(team_id, body.user_id, db):
        raise HTTPException(status_code=400, detail="该用户已在团队中")
    db.add(TeamMember(team_id=team_id, user_id=body.user_id))
    db.commit()
    return ok(None)

@router.delete("/{team_id}/members/{user_id}")
def remove_member(team_id: int, user_id: int, db: Session = Depends(get_db),
                  current_user: User = Depends(get_current_user)):
    team = db.get(Team, team_id)
    if not team: raise HTTPException(status_code=404, detail="团队不存在")
    if not is_team_leader(team, current_user):
        raise HTTPException(status_code=403, detail="仅团队负责人可操作")
    m = db.query(TeamMember).filter_by(team_id=team_id, user_id=user_id).first()
    if not m: raise HTTPException(status_code=404, detail="成员不存在")
    db.delete(m); db.commit()
    return ok(None)

@router.put("/{team_id}/transfer")
def transfer_team(team_id: int, body: AddMember, db: Session = Depends(get_db),
                  current_user: User = Depends(get_current_user)):
    team = db.get(Team, team_id)
    if not team: raise HTTPException(status_code=404, detail="团队不存在")
    if not is_team_leader(team, current_user):
        raise HTTPException(status_code=403, detail="无权限转让")
    if not is_team_member(team_id, body.user_id, db):
        raise HTTPException(status_code=400, detail="新负责人必须是团队成员")
    team.created_by = body.user_id
    db.commit()
    return ok(None)
```

- [ ] **Step 3: 运行测试**

```bash
cd backend && pytest tests/test_teams.py -v
```

Expected: 3 passed

- [ ] **Step 4: Commit**

```bash
git add backend/ && git commit -m "feat: add team routes with member management"
```

---

## Task 8: 评论与附件路由

**Files:**
- Create: `backend/routers/comments.py`
- Create: `backend/routers/attachments.py`
- Create: `backend/tests/test_attachments.py`

- [ ] **Step 1: 编写评论测试**

```python
# backend/tests/test_comments.py
def test_add_comment(client, auth_headers):
    r = client.post("/api/v1/tasks", json={"title": "任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    resp = client.post(f"/api/v1/tasks/{tid}/comments",
                       json={"content": "这是评论"}, headers=auth_headers)
    assert resp.status_code == 200
    assert resp.json()["data"]["content"] == "这是评论"

def test_delete_own_comment(client, auth_headers):
    r = client.post("/api/v1/tasks", json={"title": "任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    cr = client.post(f"/api/v1/tasks/{tid}/comments",
                     json={"content": "我的评论"}, headers=auth_headers)
    cid = cr.json()["data"]["id"]
    resp = client.delete(f"/api/v1/comments/{cid}", headers=auth_headers)
    assert resp.status_code == 200

def test_other_user_cannot_delete_comment(client, db, auth_headers):
    from models import User; from services.auth import hash_password
    bob = User(username="bob2", email="bob2@t.com", password_hash=hash_password("bob123"))
    db.add(bob); db.commit()
    r = client.post("/api/v1/tasks", json={"title": "任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    cr = client.post(f"/api/v1/tasks/{tid}/comments",
                     json={"content": "Alice的评论"}, headers=auth_headers)
    cid = cr.json()["data"]["id"]
    bob_res = client.post("/api/v1/auth/login", json={"username": "bob2", "password": "bob123"})
    bob_token = bob_res.json()["data"]["access_token"]
    resp = client.delete(f"/api/v1/comments/{cid}",
                         headers={"Authorization": f"Bearer {bob_token}"})
    assert resp.status_code == 403
```

- [ ] **Step 1b: 运行评论测试（预期失败）**

```bash
cd backend && pytest tests/test_comments.py -v
```

- [ ] **Step 2: 实现 routers/comments.py**

```python
# backend/routers/comments.py
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from sqlalchemy.orm import Session
from database import get_db
from models import Comment, Task
from dependencies import get_current_user
from models import User
from routers.tasks import can_view_task

router = APIRouter(tags=["comments"])

def ok(data): return {"code": 0, "message": "ok", "data": data}

class CommentCreate(BaseModel):
    content: str

@router.post("/tasks/{task_id}/comments")
def add_comment(task_id: int, body: CommentCreate, db: Session = Depends(get_db),
                current_user: User = Depends(get_current_user)):
    task = db.get(Task, task_id)
    if not task or not can_view_task(task, current_user, db):
        raise HTTPException(status_code=404, detail="任务不存在")
    comment = Comment(task_id=task_id, user_id=current_user.id, content=body.content)
    db.add(comment); db.commit(); db.refresh(comment)
    return ok({"id": comment.id, "content": comment.content,
               "user_id": comment.user_id, "created_at": comment.created_at.isoformat()})

@router.delete("/comments/{comment_id}")
def delete_comment(comment_id: int, db: Session = Depends(get_db),
                   current_user: User = Depends(get_current_user)):
    comment = db.get(Comment, comment_id)
    if not comment: raise HTTPException(status_code=404, detail="评论不存在")
    if comment.user_id != current_user.id and not current_user.is_admin:
        raise HTTPException(status_code=403, detail="无权限删除此评论")
    db.delete(comment); db.commit()
    return ok(None)
```

- [ ] **Step 2: 编写附件测试**

```python
# backend/tests/test_attachments.py
import io

def test_upload_and_download_attachment(client, auth_headers):
    r = client.post("/api/v1/tasks", json={"title": "有附件的任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    file_content = b"hello world"
    resp = client.post(
        f"/api/v1/tasks/{tid}/attachments",
        files={"file": ("test.txt", io.BytesIO(file_content), "text/plain")},
        headers=auth_headers,
    )
    # text/plain 不在允许列表，应该 400
    assert resp.status_code == 400

def test_upload_pdf(client, auth_headers, tmp_path):
    r = client.post("/api/v1/tasks", json={"title": "PDF任务"}, headers=auth_headers)
    tid = r.json()["data"]["id"]
    # 构造最小合法 PDF 二进制头
    pdf_content = b"%PDF-1.4 fake"
    resp = client.post(
        f"/api/v1/tasks/{tid}/attachments",
        files={"file": ("doc.pdf", io.BytesIO(pdf_content), "application/pdf")},
        headers=auth_headers,
    )
    # python-magic 会检测真实类型，fake pdf 可能通过也可能不通过
    # 此测试主要验证接口可达
    assert resp.status_code in (200, 400)
```

- [ ] **Step 3: 实现 routers/attachments.py**

```python
# backend/routers/attachments.py
import uuid
import magic
from pathlib import Path
from fastapi import APIRouter, Depends, HTTPException, UploadFile, File
from fastapi.responses import FileResponse
from sqlalchemy.orm import Session
from database import get_db
from models import Attachment, Task, Team, TeamMember
from dependencies import get_current_user
from models import User
from config import UPLOAD_DIR, MAX_UPLOAD_SIZE, ALLOWED_MIME_TYPES
from routers.tasks import can_view_task, can_edit_task

router = APIRouter(tags=["attachments"])

def ok(data): return {"code": 0, "message": "ok", "data": data}

@router.post("/tasks/{task_id}/attachments")
async def upload_attachment(task_id: int, file: UploadFile = File(...),
                             db: Session = Depends(get_db),
                             current_user: User = Depends(get_current_user)):
    task = db.get(Task, task_id)
    if not task or not can_view_task(task, current_user, db):
        raise HTTPException(status_code=404, detail="任务不存在")
    content = await file.read()
    if len(content) > MAX_UPLOAD_SIZE:
        raise HTTPException(status_code=400, detail="文件超过 20MB 限制")
    mime = magic.from_buffer(content, mime=True)
    if mime not in ALLOWED_MIME_TYPES:
        raise HTTPException(status_code=400, detail=f"不支持的文件类型: {mime}")
    stored_name = f"{uuid.uuid4()}{Path(file.filename).suffix}"
    file_path = UPLOAD_DIR / stored_name
    file_path.write_bytes(content)
    att = Attachment(
        task_id=task_id, uploaded_by=current_user.id,
        filename=file.filename, stored_name=stored_name,
        file_size=len(content), mime_type=mime,
    )
    db.add(att); db.commit(); db.refresh(att)
    return ok({"id": att.id, "filename": att.filename, "file_size": att.file_size})

@router.get("/attachments/{attachment_id}/download")
def download_attachment(attachment_id: int, db: Session = Depends(get_db),
                        current_user: User = Depends(get_current_user)):
    att = db.get(Attachment, attachment_id)
    if not att: raise HTTPException(status_code=404, detail="附件不存在")
    task = db.get(Task, att.task_id)
    if not can_view_task(task, current_user, db):
        raise HTTPException(status_code=403, detail="无权限下载")
    file_path = UPLOAD_DIR / att.stored_name
    if not file_path.exists():
        raise HTTPException(status_code=404, detail="文件已丢失")
    return FileResponse(str(file_path), filename=att.filename, media_type=att.mime_type)

@router.delete("/attachments/{attachment_id}")
def delete_attachment(attachment_id: int, db: Session = Depends(get_db),
                      current_user: User = Depends(get_current_user)):
    att = db.get(Attachment, attachment_id)
    if not att: raise HTTPException(status_code=404, detail="附件不存在")
    task = db.get(Task, att.task_id)
    is_uploader = att.uploaded_by == current_user.id
    is_task_creator = task.created_by == current_user.id
    if not (is_uploader or is_task_creator or can_edit_task(task, current_user, db)):
        raise HTTPException(status_code=403, detail="无权限删除此附件")
    file_path = UPLOAD_DIR / att.stored_name
    if file_path.exists(): file_path.unlink()
    db.delete(att); db.commit()
    return ok(None)
```

- [ ] **Step 3b: 运行评论测试（现在应该通过）**

```bash
cd backend && pytest tests/test_comments.py -v
```

Expected: 3 passed

- [ ] **Step 4: 扩展 task_to_dict 以包含 comments 和 attachments（供任务详情页使用）**

在 `backend/routers/tasks.py` 的 `task_to_dict` 函数中添加嵌套数据：

```python
def task_to_dict(task: Task, include_detail: bool = False) -> dict:
    d = {
        "id": task.id, "title": task.title, "description": task.description,
        "status": task.status, "priority": task.priority,
        "due_date": task.due_date.isoformat() if task.due_date else None,
        "created_by": task.created_by, "assigned_to": task.assigned_to,
        "team_id": task.team_id,
        "labels": [l.label for l in task.labels],
        "created_at": task.created_at.isoformat(),
        "updated_at": task.updated_at.isoformat(),
    }
    if include_detail:
        d["comments"] = [
            {"id": c.id, "user_id": c.user_id, "content": c.content,
             "created_at": c.created_at.isoformat()}
            for c in task.comments
        ]
        d["attachments"] = [
            {"id": a.id, "filename": a.filename, "file_size": a.file_size,
             "created_at": a.created_at.isoformat()}
            for a in task.attachments
        ]
    return d
```

同时更新 `GET /tasks/{task_id}` 端点，改为 `return ok(task_to_dict(task, include_detail=True))`。

- [ ] **Step 5: 运行附件测试**

```bash
cd backend && pytest tests/test_attachments.py tests/test_comments.py -v
```

- [ ] **Step 6: Commit**

```bash
git add backend/ && git commit -m "feat: add comments and attachments routes, extend task detail API"
```

---

## Task 9: 管理员路由 + SMTP 配置

**Files:**
- Create: `backend/routers/users.py`
- Create: `backend/routers/settings.py`
- Create: `backend/services/email.py`

- [ ] **Step 1: 实现 routers/users.py（管理员用户管理）**

```python
# backend/routers/users.py
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel, EmailStr
from sqlalchemy.orm import Session
from database import get_db
from models import User
from services.auth import hash_password
from dependencies import require_admin

router = APIRouter(prefix="/admin/users", tags=["admin"])

def ok(data): return {"code": 0, "message": "ok", "data": data}

def user_to_dict(u: User):
    return {"id": u.id, "username": u.username, "email": u.email,
            "is_admin": u.is_admin, "is_active": u.is_active,
            "email_notify": u.email_notify}

class UserCreate(BaseModel):
    username: str
    email: str
    password: str
    is_admin: bool = False

class UserUpdate(BaseModel):
    email: str | None = None
    is_admin: bool | None = None
    is_active: bool | None = None
    email_notify: bool | None = None
    password: str | None = None

@router.get("")
def list_users(db: Session = Depends(get_db), _=Depends(require_admin)):
    users = db.query(User).all()
    return ok([user_to_dict(u) for u in users])

@router.post("")
def create_user(body: UserCreate, db: Session = Depends(get_db), _=Depends(require_admin)):
    if db.query(User).filter(User.username == body.username).first():
        raise HTTPException(status_code=400, detail="用户名已存在")
    if db.query(User).filter(User.email == body.email).first():
        raise HTTPException(status_code=400, detail="邮箱已存在")
    user = User(username=body.username, email=body.email,
                password_hash=hash_password(body.password), is_admin=body.is_admin)
    db.add(user); db.commit(); db.refresh(user)
    return ok(user_to_dict(user))

@router.put("/{user_id}")
def update_user(user_id: int, body: UserUpdate, db: Session = Depends(get_db),
                _=Depends(require_admin)):
    user = db.get(User, user_id)
    if not user: raise HTTPException(status_code=404, detail="用户不存在")
    if body.email is not None: user.email = body.email
    if body.is_admin is not None: user.is_admin = body.is_admin
    if body.is_active is not None: user.is_active = body.is_active
    if body.email_notify is not None: user.email_notify = body.email_notify
    if body.password is not None: user.password_hash = hash_password(body.password)
    db.commit()
    return ok(user_to_dict(user))
```

- [ ] **Step 2: 实现 services/email.py**

```python
# backend/services/email.py
import smtplib
import logging
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart
from cryptography.fernet import Fernet
import base64
from config import SECRET_KEY

logger = logging.getLogger(__name__)

def _get_fernet() -> Fernet:
    # 用 SECRET_KEY 派生 32 字节 base64 key
    key = base64.urlsafe_b64encode(SECRET_KEY.encode()[:32].ljust(32, b'0'))
    return Fernet(key)

def encrypt_password(plain: str) -> str:
    return _get_fernet().encrypt(plain.encode()).decode()

def decrypt_password(cipher: str) -> str:
    return _get_fernet().decrypt(cipher.encode()).decode()

def get_smtp_config(db) -> dict | None:
    from models import SystemSetting
    keys = ["smtp_host", "smtp_port", "smtp_user", "smtp_password", "smtp_from", "email_send_hour"]
    settings = {s.key: s.value for s in db.query(SystemSetting).filter(SystemSetting.key.in_(keys)).all()}
    if not settings.get("smtp_host"):
        return None
    if settings.get("smtp_password"):
        try:
            settings["smtp_password"] = decrypt_password(settings["smtp_password"])
        except Exception:
            pass
    return settings

def send_email(to: str, subject: str, body: str, config: dict):
    msg = MIMEMultipart("alternative")
    msg["Subject"] = subject
    msg["From"] = config["smtp_from"]
    msg["To"] = to
    msg.attach(MIMEText(body, "html", "utf-8"))
    port = int(config.get("smtp_port", 25))
    with smtplib.SMTP(config["smtp_host"], port, timeout=10) as server:
        if config.get("smtp_user") and config.get("smtp_password"):
            server.login(config["smtp_user"], config["smtp_password"])
        server.send_message(msg)
    logger.info(f"邮件已发送至 {to}")
```

- [ ] **Step 3: 实现 routers/settings.py**

```python
# backend/routers/settings.py
from datetime import datetime
from fastapi import APIRouter, Depends
from pydantic import BaseModel
from sqlalchemy.orm import Session
from database import get_db
from models import SystemSetting
from dependencies import require_admin
from services.email import encrypt_password

router = APIRouter(prefix="/admin/settings", tags=["admin"])

def ok(data): return {"code": 0, "message": "ok", "data": data}

SMTP_KEYS = ["smtp_host", "smtp_port", "smtp_user", "smtp_password", "smtp_from", "email_send_hour"]

class SettingsUpdate(BaseModel):
    smtp_host: str | None = None
    smtp_port: str | None = None
    smtp_user: str | None = None
    smtp_password: str | None = None
    smtp_from: str | None = None
    email_send_hour: str | None = None

@router.get("")
def get_settings(db: Session = Depends(get_db), _=Depends(require_admin)):
    rows = {s.key: s.value for s in db.query(SystemSetting).filter(SystemSetting.key.in_(SMTP_KEYS)).all()}
    if "smtp_password" in rows and rows["smtp_password"]:
        rows["smtp_password"] = "******"  # 不回传明文
    return ok(rows)

@router.put("")
def update_settings(body: SettingsUpdate, db: Session = Depends(get_db), _=Depends(require_admin)):
    updates = body.model_dump(exclude_none=True)
    if "smtp_password" in updates:
        updates["smtp_password"] = encrypt_password(updates["smtp_password"])
    for key, value in updates.items():
        row = db.get(SystemSetting, key)
        if row:
            row.value = value
            row.updated_at = datetime.utcnow()
        else:
            db.add(SystemSetting(key=key, value=value))
    db.commit()
    # 通知调度器重新读取 email_send_hour
    if "email_send_hour" in updates:
        from scheduler import reschedule_email_job
        reschedule_email_job(int(updates["email_send_hour"]))
    return ok(None)
```

- [ ] **Step 4: Commit**

```bash
git add backend/ && git commit -m "feat: add admin user/settings routes and email service"
```

---

## Task 10: 邮件调度器

**Files:**
- Create: `backend/scheduler.py`
- Update: `backend/main.py`

- [ ] **Step 1: 实现 scheduler.py**

```python
# backend/scheduler.py
import logging
from datetime import date
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger

logger = logging.getLogger(__name__)
scheduler = BackgroundScheduler()

def send_daily_reminders():
    from database import SessionLocal
    from models import Task, Team, User
    from services.email import get_smtp_config, send_email

    db = SessionLocal()
    try:
        config = get_smtp_config(db)
        if not config:
            logger.warning("SMTP 未配置，跳过邮件发送")
            return

        today = date.today()
        tasks = db.query(Task).filter(
            Task.due_date <= today,
            Task.status != "done"
        ).all()

        # 按负责人聚合
        assignee_tasks: dict[int, list] = {}
        for t in tasks:
            if t.assigned_to:
                assignee_tasks.setdefault(t.assigned_to, []).append(t)

        # 按团队负责人聚合（team_id 不为空的任务）
        leader_tasks: dict[int, list] = {}
        for t in tasks:
            if t.team_id:
                team = db.get(Team, t.team_id)
                if team:
                    leader_tasks.setdefault(team.created_by, []).append(t)

        def send_to_user(user_id: int, task_list: list):
            user = db.get(User, user_id)
            if not user or not user.is_active or not user.email_notify:
                return
            rows = "".join(
                f"<li><b>{t.title}</b> — 截止 {t.due_date} — 状态: {t.status}</li>"
                for t in task_list
            )
            body = f"<h3>今日待处理任务</h3><ul>{rows}</ul>"
            try:
                send_email(user.email, "【待办提醒】今日任务汇总", body, config)
            except Exception as e:
                logger.error(f"发送邮件给 {user.email} 失败: {e}")

        sent = set()
        for uid, tlist in assignee_tasks.items():
            send_to_user(uid, tlist)
            sent.add(uid)
        for uid, tlist in leader_tasks.items():
            send_to_user(uid, tlist)
    finally:
        db.close()

def reschedule_email_job(hour: int):
    scheduler.reschedule_job("daily_email", trigger=CronTrigger(hour=hour, minute=0))
    logger.info(f"邮件提醒已调整为每天 {hour}:00")

def start_scheduler(hour: int = 8):
    scheduler.add_job(send_daily_reminders, CronTrigger(hour=hour, minute=0), id="daily_email")
    scheduler.start()
    logger.info(f"调度器已启动，每天 {hour}:00 发送邮件")
```

- [ ] **Step 2: 更新 main.py 启动调度器**

将 `main.py` 中的整个 `startup()` 函数**替换**为：
```python
@app.on_event("startup")
def startup():
    create_tables()
    from scheduler import start_scheduler
    from database import SessionLocal
    db = SessionLocal()
    try:
        from models import SystemSetting
        row = db.get(SystemSetting, "email_send_hour")
        hour = int(row.value) if row and row.value else 8
    finally:
        db.close()
    start_scheduler(hour)
```
（注意：完整替换原有的 `startup()` 函数，不要追加）

- [ ] **Step 3: Commit**

```bash
git add backend/ && git commit -m "feat: add APScheduler daily email reminder job"
```

---

## Task 11: 初始管理员脚本

**Files:**
- Create: `backend/scripts/init_admin.py`

- [ ] **Step 1: 实现 init_admin.py**

```python
# backend/scripts/init_admin.py
import sys
import argparse
import getpass
sys.path.insert(0, ".")  # 确保能导入 backend 模块

from database import SessionLocal, create_tables
from models import User
from services.auth import hash_password

def main():
    parser = argparse.ArgumentParser(description="初始化管理员账号")
    parser.add_argument("--username", required=True)
    parser.add_argument("--email", required=True)
    args = parser.parse_args()

    create_tables()
    db = SessionLocal()
    try:
        existing = db.query(User).filter(User.username == args.username).first()
        if existing:
            print(f"用户 '{args.username}' 已存在，跳过创建。")
            return
        password = getpass.getpass("请输入管理员密码: ")
        if len(password) < 6:
            print("密码至少需要 6 位")
            sys.exit(1)
        user = User(
            username=args.username, email=args.email,
            password_hash=hash_password(password), is_admin=True
        )
        db.add(user); db.commit()
        print(f"管理员账号 '{args.username}' 创建成功。")
    finally:
        db.close()

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行完整后端测试**

```bash
cd backend && pytest tests/ -v --tb=short
```

Expected: 全部通过

- [ ] **Step 3: Commit**

```bash
git add backend/ && git commit -m "feat: add init_admin script and complete backend"
```

---

## Task 12: 前端项目初始化

**Files:**
- Create: `frontend/` (Vite + Vue 3 项目)

- [ ] **Step 1: 创建 Vite 项目**

```bash
cd /root/todo && npm create vite@latest frontend -- --template vue
cd frontend && npm install
```

- [ ] **Step 2: 安装依赖**

```bash
cd frontend
npm install element-plus @element-plus/icons-vue
npm install axios pinia vue-router@4
npm install vuedraggable@next
```

- [ ] **Step 3: 配置 vite.config.js（代理后端 API）**

```javascript
// frontend/vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': { target: 'http://localhost:8000', changeOrigin: true }
    }
  }
})
```

- [ ] **Step 4: 配置 main.js**

```javascript
// frontend/src/main.js
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as Icons from '@element-plus/icons-vue'
import App from './App.vue'
import router from './router/index.js'

const app = createApp(App)
app.use(createPinia())
app.use(router)
app.use(ElementPlus)
Object.entries(Icons).forEach(([name, comp]) => app.component(name, comp))
app.mount('#app')
```

- [ ] **Step 5: 创建 API 客户端**

```javascript
// frontend/src/api/index.js
import axios from 'axios'

const api = axios.create({ baseURL: '/api/v1', withCredentials: true })

let accessToken = null

export function setToken(token) { accessToken = token }
export function getToken() { return accessToken }

api.interceptors.request.use(config => {
  if (accessToken) config.headers.Authorization = `Bearer ${accessToken}`
  return config
})

api.interceptors.response.use(
  res => res.data,
  async err => {
    if (err.response?.status === 401 && !err.config._retry) {
      err.config._retry = true
      try {
        const res = await api.post('/auth/refresh')
        setToken(res.data.access_token)
        err.config.headers.Authorization = `Bearer ${accessToken}`
        return api(err.config)
      } catch {
        setToken(null)
        window.location.href = '/login'
      }
    }
    return Promise.reject(err)
  }
)

export default api
```

- [ ] **Step 6: 创建 Pinia user store**

```javascript
// frontend/src/stores/user.js
import { defineStore } from 'pinia'
import api, { setToken } from '../api/index.js'

export const useUserStore = defineStore('user', {
  state: () => ({ user: null }),
  getters: { isLoggedIn: s => !!s.user, isAdmin: s => s.user?.is_admin },
  actions: {
    async login(username, password) {
      const res = await api.post('/auth/login', { username, password })
      setToken(res.data.access_token)
      // 获取当前用户信息（暂存 username）
      this.user = { username }
      return res
    },
    async logout() {
      await api.post('/auth/logout')
      setToken(null)
      this.user = null
    }
  }
})
```

- [ ] **Step 7: 创建路由**

```javascript
// frontend/src/router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '../stores/user.js'

const routes = [
  { path: '/login', component: () => import('../views/Login.vue'), meta: { public: true } },
  { path: '/my-tasks', component: () => import('../views/MyTasks.vue') },
  { path: '/team/:id', component: () => import('../views/TeamTasks.vue') },
  { path: '/task/:id', component: () => import('../views/TaskDetail.vue') },
  { path: '/team/:id/manage', component: () => import('../views/TeamManage.vue') },
  { path: '/admin/users', component: () => import('../views/admin/UserManage.vue'), meta: { requireAdmin: true } },
  { path: '/admin/settings', component: () => import('../views/admin/SystemSettings.vue'), meta: { requireAdmin: true } },
  { path: '/', redirect: '/my-tasks' },
]

const router = createRouter({ history: createWebHistory(), routes })

router.beforeEach((to) => {
  const store = useUserStore()
  if (!to.meta.public && !store.isLoggedIn) return '/login'
  if (to.meta.requireAdmin && !store.isAdmin) return '/my-tasks'
})

export default router
```

- [ ] **Step 8: Commit**

```bash
git add frontend/ && git commit -m "feat: frontend project init with Vue 3, Element Plus, Pinia, router"
```

---

## Task 13: 登录页

**Files:**
- Create: `frontend/src/views/Login.vue`
- Update: `frontend/src/App.vue`

- [ ] **Step 1: 创建 App.vue**

```vue
<!-- frontend/src/App.vue -->
<template>
  <router-view />
</template>

<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body { background: #F5F0EB; font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif; color: #2C2C2C; }
</style>
```

- [ ] **Step 2: 实现 Login.vue**

```vue
<!-- frontend/src/views/Login.vue -->
<template>
  <div class="login-wrap">
    <el-card class="login-card">
      <h2 class="title">团队待办</h2>
      <el-form :model="form" @submit.prevent="handleLogin">
        <el-form-item>
          <el-input v-model="form.username" placeholder="用户名" prefix-icon="User" />
        </el-form-item>
        <el-form-item>
          <el-input v-model="form.password" type="password" placeholder="密码"
                    prefix-icon="Lock" show-password />
        </el-form-item>
        <el-button type="primary" native-type="submit" :loading="loading" class="btn-login">
          登录
        </el-button>
        <p v-if="error" class="error">{{ error }}</p>
      </el-form>
    </el-card>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '../stores/user.js'

const router = useRouter()
const store = useUserStore()
const form = reactive({ username: '', password: '' })
const loading = ref(false)
const error = ref('')

async function handleLogin() {
  loading.value = true; error.value = ''
  try {
    await store.login(form.username, form.password)
    router.push('/my-tasks')
  } catch (e) {
    error.value = e.response?.data?.detail || '登录失败'
  } finally {
    loading.value = false
  }
}
</script>

<style scoped>
.login-wrap { min-height: 100vh; display: flex; align-items: center; justify-content: center; }
.login-card { width: 360px; border-radius: 12px; }
.title { text-align: center; margin-bottom: 24px; font-size: 22px; color: #E8572A; }
.btn-login { width: 100%; background: #E8572A; border-color: #E8572A; }
.btn-login:hover { background: #F4A259; border-color: #F4A259; }
.error { color: #E8572A; margin-top: 8px; font-size: 13px; }
</style>
```

- [ ] **Step 3: 启动前端验证登录页**

```bash
cd frontend && npm run dev
```

访问 `http://localhost:5173/login`，确认登录页正常显示。

- [ ] **Step 4: Commit**

```bash
git add frontend/ && git commit -m "feat: add login page with brand colors"
```

---

## Task 14: 我的任务看板

**Files:**
- Create: `frontend/src/views/MyTasks.vue`
- Create: `frontend/src/components/TaskCard.vue`
- Create: `frontend/src/components/TaskBoard.vue`

- [ ] **Step 1: 创建 TaskCard.vue**

```vue
<!-- frontend/src/components/TaskCard.vue -->
<template>
  <div class="task-card" :class="urgentClass">
    <div class="card-title">{{ task.title }}</div>
    <div class="card-meta">
      <el-tag size="small" :type="priorityType">{{ priorityLabel }}</el-tag>
      <span v-if="task.due_date" class="due">{{ task.due_date }}</span>
    </div>
    <div v-if="task.labels?.length" class="labels">
      <el-tag v-for="l in task.labels" :key="l" size="small" class="label-tag">{{ l }}</el-tag>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue'
const props = defineProps({ task: Object })

const priorityMap = { high: ['danger', '高'], medium: ['warning', '中'], low: ['info', '低'] }
const priorityType = computed(() => priorityMap[props.task.priority]?.[0] || 'info')
const priorityLabel = computed(() => priorityMap[props.task.priority]?.[1] || props.task.priority)

const urgentClass = computed(() => {
  if (!props.task.due_date) return ''
  const diff = (new Date(props.task.due_date) - new Date()) / 86400000
  return diff <= 1 && props.task.status !== 'done' ? 'urgent' : ''
})
</script>

<style scoped>
.task-card { background: #fff; border-radius: 8px; padding: 12px; margin-bottom: 8px;
             border-left: 4px solid #D9D0C7; cursor: grab; box-shadow: 0 1px 4px #0001; }
.task-card.urgent { border-left-color: #E8572A; }
.card-title { font-weight: 600; margin-bottom: 6px; }
.card-meta { display: flex; align-items: center; gap: 8px; font-size: 12px; color: #888; }
.due { color: #E8572A; }
.labels { margin-top: 6px; display: flex; flex-wrap: wrap; gap: 4px; }
.label-tag { background: #F4A25920; border-color: #F4A259; color: #c07020; }
</style>
```

- [ ] **Step 2: 创建 TaskBoard.vue（拖拽看板）**

```vue
<!-- frontend/src/components/TaskBoard.vue -->
<template>
  <div class="board">
    <div v-for="col in columns" :key="col.status" class="column">
      <div class="col-header">{{ col.label }}
        <el-badge :value="tasksByStatus[col.status]?.length || 0" type="info" />
      </div>
      <draggable
        v-model="tasksByStatus[col.status]"
        group="tasks"
        item-key="id"
        @end="onDragEnd(col.status, $event)"
      >
        <template #item="{ element }">
          <task-card :task="element" @click="$emit('task-click', element)" />
        </template>
      </draggable>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue'
import draggable from 'vuedraggable'
import TaskCard from './TaskCard.vue'

const props = defineProps({ tasks: Array })
const emit = defineEmits(['update-status', 'task-click'])

const columns = [
  { status: 'pending', label: '待处理' },
  { status: 'in_progress', label: '进行中' },
  { status: 'done', label: '已完成' },
]

const tasksByStatus = computed(() => {
  const map = { pending: [], in_progress: [], done: [] }
  for (const t of props.tasks || []) map[t.status]?.push(t)
  return map
})

function onDragEnd(toStatus, evt) {
  const task = evt.item?.__draggable_context?.element
  if (task && task.status !== toStatus) {
    emit('update-status', { taskId: task.id, status: toStatus })
  }
}
</script>

<style scoped>
.board { display: flex; gap: 16px; align-items: flex-start; }
.column { flex: 1; background: #F5F0EB; border-radius: 10px; padding: 12px; min-height: 300px; }
.col-header { font-weight: 700; margin-bottom: 12px; font-size: 15px; color: #2C2C2C;
              display: flex; align-items: center; gap: 8px; }
</style>
```

- [ ] **Step 3: 实现 MyTasks.vue**

```vue
<!-- frontend/src/views/MyTasks.vue -->
<template>
  <div class="page">
    <div class="header">
      <h2>我的任务</h2>
      <el-button type="primary" @click="showCreate = true">+ 新建任务</el-button>
    </div>
    <task-board :tasks="tasks" @update-status="updateStatus" @task-click="goDetail" />

    <el-dialog v-model="showCreate" title="新建任务" width="500px">
      <el-form :model="newTask" label-width="80px">
        <el-form-item label="标题"><el-input v-model="newTask.title" /></el-form-item>
        <el-form-item label="优先级">
          <el-select v-model="newTask.priority">
            <el-option value="high" label="高" />
            <el-option value="medium" label="中" />
            <el-option value="low" label="低" />
          </el-select>
        </el-form-item>
        <el-form-item label="截止日期">
          <el-date-picker v-model="newTask.due_date" type="date" value-format="YYYY-MM-DD" />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="showCreate = false">取消</el-button>
        <el-button type="primary" @click="createTask">创建</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup>
import { ref, onMounted, reactive } from 'vue'
import { useRouter } from 'vue-router'
import api from '../api/index.js'
import TaskBoard from '../components/TaskBoard.vue'

const router = useRouter()
const tasks = ref([])
const showCreate = ref(false)
const newTask = reactive({ title: '', priority: 'medium', due_date: null })

async function loadTasks() {
  const res = await api.get('/tasks')
  tasks.value = res.data.items
}

async function createTask() {
  await api.post('/tasks', newTask)
  showCreate.value = false
  newTask.title = ''; newTask.priority = 'medium'; newTask.due_date = null
  await loadTasks()
}

async function updateStatus({ taskId, status }) {
  await api.patch(`/tasks/${taskId}/status`, { status })
  await loadTasks()
}

function goDetail(task) { router.push(`/task/${task.id}`) }

onMounted(loadTasks)
</script>

<style scoped>
.page { padding: 24px; }
.header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
h2 { color: #E8572A; font-size: 20px; }
</style>
```

- [ ] **Step 4: Commit**

```bash
git add frontend/ && git commit -m "feat: add task board with drag-and-drop kanban"
```

---

## Task 15: 任务详情页

**Files:**
- Create: `frontend/src/views/TaskDetail.vue`
- Create: `frontend/src/components/CommentList.vue`
- Create: `frontend/src/components/FileUpload.vue`

- [ ] **Step 1: 创建 CommentList.vue**

```vue
<!-- frontend/src/components/CommentList.vue -->
<template>
  <div>
    <div v-for="c in comments" :key="c.id" class="comment">
      <span class="author">{{ c.user_id }}</span>
      <span class="time">{{ c.created_at }}</span>
      <p>{{ c.content }}</p>
    </div>
    <el-input v-model="content" type="textarea" placeholder="写评论..." :rows="3" />
    <el-button type="primary" size="small" @click="submit" style="margin-top:8px">发表</el-button>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import api from '../api/index.js'

const props = defineProps({ taskId: Number, comments: Array })
const emit = defineEmits(['refresh'])
const content = ref('')

async function submit() {
  if (!content.value.trim()) return
  await api.post(`/tasks/${props.taskId}/comments`, { content: content.value })
  content.value = ''
  emit('refresh')
}
</script>

<style scoped>
.comment { background: #fafafa; border-radius: 6px; padding: 8px 12px; margin-bottom: 8px; }
.author { font-weight: 600; margin-right: 8px; color: #E8572A; }
.time { font-size: 12px; color: #aaa; }
</style>
```

- [ ] **Step 2: 创建 FileUpload.vue**

```vue
<!-- frontend/src/components/FileUpload.vue -->
<template>
  <div>
    <el-upload :action="`/api/v1/tasks/${taskId}/attachments`"
               :headers="headers" :on-success="onSuccess" :on-error="onError"
               :show-file-list="false" :before-upload="beforeUpload">
      <el-button size="small">上传附件</el-button>
    </el-upload>
    <div v-for="att in attachments" :key="att.id" class="att-item">
      <el-icon><Paperclip /></el-icon>
      <a :href="`/api/v1/attachments/${att.id}/download`" target="_blank">{{ att.filename }}</a>
      <span class="size">{{ (att.file_size / 1024).toFixed(1) }}KB</span>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue'
import { getToken } from '../api/index.js'

const props = defineProps({ taskId: Number, attachments: Array })
const emit = defineEmits(['refresh'])
const headers = computed(() => ({ Authorization: `Bearer ${getToken()}` }))

import { ElMessage } from 'element-plus'

function beforeUpload(file) {
  if (file.size > 20 * 1024 * 1024) {
    ElMessage.error('文件不能超过 20MB'); return false
  }
  return true
}
function onSuccess() { emit('refresh') }
function onError() { ElMessage.error('上传失败，请检查文件类型') }
</script>

<style scoped>
.att-item { display: flex; align-items: center; gap: 8px; padding: 4px 0; font-size: 13px; }
.size { color: #aaa; font-size: 12px; }
</style>
```

- [ ] **Step 3: 实现 TaskDetail.vue**

```vue
<!-- frontend/src/views/TaskDetail.vue -->
<template>
  <div class="page" v-if="task">
    <el-page-header @back="router.back()" :content="task.title" />
    <el-card class="detail-card">
      <el-descriptions :column="2" border>
        <el-descriptions-item label="状态">
          <el-select v-model="task.status" @change="updateStatus">
            <el-option value="pending" label="待处理" />
            <el-option value="in_progress" label="进行中" />
            <el-option value="done" label="已完成" />
          </el-select>
        </el-descriptions-item>
        <el-descriptions-item label="优先级">{{ task.priority }}</el-descriptions-item>
        <el-descriptions-item label="截止日期">{{ task.due_date || '—' }}</el-descriptions-item>
        <el-descriptions-item label="标签">
          <el-tag v-for="l in task.labels" :key="l" size="small" class="m-1">{{ l }}</el-tag>
        </el-descriptions-item>
        <el-descriptions-item label="描述" :span="2">{{ task.description || '—' }}</el-descriptions-item>
      </el-descriptions>
    </el-card>

    <el-card class="section-card">
      <template #header>附件</template>
      <file-upload :task-id="task.id" :attachments="task.attachments || []" @refresh="loadTask" />
    </el-card>

    <el-card class="section-card">
      <template #header>评论</template>
      <comment-list :task-id="task.id" :comments="task.comments || []" @refresh="loadTask" />
    </el-card>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import api from '../api/index.js'
import CommentList from '../components/CommentList.vue'
import FileUpload from '../components/FileUpload.vue'

const route = useRoute(); const router = useRouter()
const task = ref(null)

async function loadTask() {
  const res = await api.get(`/tasks/${route.params.id}`)
  task.value = res.data
}

async function updateStatus(status) {
  await api.patch(`/tasks/${task.value.id}/status`, { status })
}

onMounted(loadTask)
</script>

<style scoped>
.page { padding: 24px; }
.detail-card, .section-card { margin-top: 16px; }
.m-1 { margin: 2px; }
</style>
```

- [ ] **Step 4: Commit**

```bash
git add frontend/ && git commit -m "feat: add task detail page with comments and attachments"
```

---

## Task 16: 团队任务页 + 团队管理页

**Files:**
- Create: `frontend/src/views/TeamTasks.vue`
- Create: `frontend/src/views/TeamManage.vue`

- [ ] **Step 1: 实现 TeamTasks.vue**

```vue
<!-- frontend/src/views/TeamTasks.vue -->
<template>
  <div class="page">
    <div class="header">
      <h2>{{ teamName }}</h2>
      <div>
        <el-button @click="router.push(`/team/${teamId}/manage`)">管理成员</el-button>
        <el-button type="primary" @click="showCreate = true">+ 新建任务</el-button>
      </div>
    </div>
    <task-board :tasks="tasks" @update-status="updateStatus" @task-click="goDetail" />

    <el-dialog v-model="showCreate" title="新建团队任务" width="500px">
      <el-form :model="newTask" label-width="90px">
        <el-form-item label="标题"><el-input v-model="newTask.title" /></el-form-item>
        <el-form-item label="负责人">
          <el-select v-model="newTask.assigned_to" clearable placeholder="不指定">
            <el-option v-for="m in members" :key="m.user_id"
                       :value="m.user_id" :label="m.username" />
          </el-select>
        </el-form-item>
        <el-form-item label="优先级">
          <el-select v-model="newTask.priority">
            <el-option value="high" label="高" /><el-option value="medium" label="中" /><el-option value="low" label="低" />
          </el-select>
        </el-form-item>
        <el-form-item label="截止日期">
          <el-date-picker v-model="newTask.due_date" type="date" value-format="YYYY-MM-DD" />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="showCreate = false">取消</el-button>
        <el-button type="primary" @click="createTask">创建</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup>
import { ref, onMounted, reactive } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import api from '../api/index.js'
import TaskBoard from '../components/TaskBoard.vue'

const route = useRoute(); const router = useRouter()
const teamId = route.params.id
const teamName = ref(''); const tasks = ref([]); const members = ref([])
const showCreate = ref(false)
const newTask = reactive({ title: '', priority: 'medium', due_date: null, assigned_to: null })

async function load() {
  const [teamRes, taskRes] = await Promise.all([
    api.get(`/teams/${teamId}`), api.get(`/teams/${teamId}/tasks`)
  ])
  teamName.value = teamRes.data.name
  members.value = teamRes.data.members
  tasks.value = taskRes.data.items
}

async function createTask() {
  await api.post('/tasks', { ...newTask, team_id: Number(teamId) })
  showCreate.value = false; await load()
}

async function updateStatus({ taskId, status }) {
  await api.patch(`/tasks/${taskId}/status`, { status }); await load()
}

function goDetail(task) { router.push(`/task/${task.id}`) }

onMounted(load)
</script>

<style scoped>
.page { padding: 24px; }
.header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
h2 { color: #E8572A; font-size: 20px; }
</style>
```

- [ ] **Step 2: 实现 TeamManage.vue**

```vue
<!-- frontend/src/views/TeamManage.vue -->
<template>
  <div class="page">
    <el-page-header @back="router.back()" content="团队成员管理" />
    <el-card style="margin-top:16px">
      <el-table :data="members">
        <el-table-column prop="username" label="用户名" />
        <el-table-column prop="email" label="邮箱" />
        <el-table-column label="操作">
          <template #default="{ row }">
            <el-button type="danger" size="small" @click="removeMember(row.user_id)">移除</el-button>
          </template>
        </el-table-column>
      </el-table>
      <div style="margin-top:16px;display:flex;gap:8px">
        <el-input v-model="newUserId" placeholder="输入用户 ID" style="width:200px" />
        <el-button type="primary" @click="addMember">添加成员</el-button>
      </div>
    </el-card>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import api from '../api/index.js'
import { ElMessage } from 'element-plus'

const route = useRoute(); const router = useRouter()
const teamId = route.params.id
const members = ref([]); const newUserId = ref('')

async function load() {
  const res = await api.get(`/teams/${teamId}`)
  members.value = res.data.members
}

async function addMember() {
  if (!newUserId.value) return
  try {
    await api.post(`/teams/${teamId}/members`, { user_id: Number(newUserId.value) })
    newUserId.value = ''; await load()
  } catch (e) { ElMessage.error(e.response?.data?.detail || '添加失败') }
}

async function removeMember(userId) {
  await api.delete(`/teams/${teamId}/members/${userId}`); await load()
}

onMounted(load)
</script>

<style scoped>
.page { padding: 24px; }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add frontend/ && git commit -m "feat: add team tasks page and team manage page"
```

---

## Task 17: 管理员页面

**Files:**
- Create: `frontend/src/views/admin/UserManage.vue`
- Create: `frontend/src/views/admin/SystemSettings.vue`

- [ ] **Step 1: 实现 UserManage.vue**

```vue
<!-- frontend/src/views/admin/UserManage.vue -->
<template>
  <div class="page">
    <div class="header">
      <h2>用户管理</h2>
      <el-button type="primary" @click="showCreate = true">+ 新建用户</el-button>
    </div>
    <el-table :data="users" border>
      <el-table-column prop="username" label="用户名" />
      <el-table-column prop="email" label="邮箱" />
      <el-table-column label="管理员">
        <template #default="{ row }">
          <el-tag :type="row.is_admin ? 'warning' : 'info'">{{ row.is_admin ? '是' : '否' }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column label="状态">
        <template #default="{ row }">
          <el-tag :type="row.is_active ? 'success' : 'danger'">{{ row.is_active ? '启用' : '停用' }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column label="操作">
        <template #default="{ row }">
          <el-button size="small" @click="toggleActive(row)">
            {{ row.is_active ? '停用' : '启用' }}
          </el-button>
        </template>
      </el-table-column>
    </el-table>

    <el-dialog v-model="showCreate" title="新建用户" width="400px">
      <el-form :model="newUser" label-width="80px">
        <el-form-item label="用户名"><el-input v-model="newUser.username" /></el-form-item>
        <el-form-item label="邮箱"><el-input v-model="newUser.email" /></el-form-item>
        <el-form-item label="密码"><el-input v-model="newUser.password" type="password" /></el-form-item>
        <el-form-item label="管理员"><el-switch v-model="newUser.is_admin" /></el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="showCreate = false">取消</el-button>
        <el-button type="primary" @click="createUser">创建</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup>
import { ref, onMounted, reactive } from 'vue'
import api from '../../api/index.js'

const users = ref([]); const showCreate = ref(false)
const newUser = reactive({ username: '', email: '', password: '', is_admin: false })

async function load() { const res = await api.get('/admin/users'); users.value = res.data }

async function createUser() {
  await api.post('/admin/users', newUser); showCreate.value = false; await load()
}

async function toggleActive(user) {
  await api.put(`/admin/users/${user.id}`, { is_active: !user.is_active }); await load()
}

onMounted(load)
</script>

<style scoped>
.page { padding: 24px; }
.header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; }
h2 { color: #E8572A; font-size: 20px; }
</style>
```

- [ ] **Step 2: 实现 SystemSettings.vue**

```vue
<!-- frontend/src/views/admin/SystemSettings.vue -->
<template>
  <div class="page">
    <h2>系统设置 — 邮件服务器</h2>
    <el-card style="margin-top:16px;max-width:500px">
      <el-form :model="form" label-width="120px">
        <el-form-item label="SMTP 主机"><el-input v-model="form.smtp_host" /></el-form-item>
        <el-form-item label="SMTP 端口"><el-input v-model="form.smtp_port" /></el-form-item>
        <el-form-item label="发件人地址"><el-input v-model="form.smtp_from" /></el-form-item>
        <el-form-item label="账号"><el-input v-model="form.smtp_user" /></el-form-item>
        <el-form-item label="密码">
          <el-input v-model="form.smtp_password" type="password" placeholder="留空表示不修改" />
        </el-form-item>
        <el-form-item label="发送时间（小时）">
          <el-input-number v-model="form.email_send_hour" :min="0" :max="23" />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="save">保存</el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>

<script setup>
import { reactive, onMounted } from 'vue'
import api from '../../api/index.js'
import { ElMessage } from 'element-plus'

const form = reactive({
  smtp_host: '', smtp_port: '25', smtp_from: '',
  smtp_user: '', smtp_password: '', email_send_hour: 8
})

async function load() {
  const res = await api.get('/admin/settings')
  Object.assign(form, res.data)
  form.smtp_password = ''  // 不显示密码
  form.email_send_hour = Number(form.email_send_hour) || 8
}

async function save() {
  const payload = { ...form }
  if (!payload.smtp_password) delete payload.smtp_password  // 空则不更新
  payload.email_send_hour = String(payload.email_send_hour)
  await api.put('/admin/settings', payload)
  ElMessage.success('保存成功')
}

onMounted(load)
</script>

<style scoped>
.page { padding: 24px; }
h2 { color: #E8572A; font-size: 20px; margin-bottom: 8px; }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add frontend/ && git commit -m "feat: add admin user management and SMTP settings pages"
```

---

## Task 18: 联调验证与运行所有测试

- [ ] **Step 1: 运行完整后端测试**

```bash
cd backend && pytest tests/ -v --tb=short
```

Expected: 全部通过

- [ ] **Step 2: 启动后端**

```bash
cd backend && uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

- [ ] **Step 3: 启动前端**

```bash
cd frontend && npm run dev
```

- [ ] **Step 4: 端对端验证清单**

- [ ] 访问 `http://localhost:5173/login`，使用管理员账号登录
- [ ] 创建个人任务，拖拽改变状态
- [ ] 进入任务详情，发表评论
- [ ] 上传 PDF 附件，确认下载正常
- [ ] 创建团队，添加成员
- [ ] 在管理员后台创建用户，停用用户
- [ ] 配置 SMTP，验证保存成功
- [ ] 访问 `http://localhost:8000/docs` 确认 API 文档正常

- [ ] **Step 5: 前端构建**

```bash
cd frontend && npm run build
```

Expected: `dist/` 目录生成，无构建错误

- [ ] **Step 6: 最终 Commit**

```bash
git add . && git commit -m "feat: complete todo app frontend and backend integration"
```

---

## 附录：Nginx 部署配置参考

```nginx
server {
    listen 80;
    server_name _;

    root /path/to/frontend/dist;
    index index.html;

    # 前端路由（SPA）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```
